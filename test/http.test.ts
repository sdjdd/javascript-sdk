import 'should';
import { HTTPRequest } from '../src/core/http';

describe('HTTPRequest', function () {
  describe('.url', function () {
    // const baseURL = 'http://base.example.com';
    // const path = '/path/to/somewhere';
    // it('should concat baseURL and path', function () {
    //   const req = new HTTPRequest({ baseURL, path });
    //   req.url.should.eql(baseURL + path);
    // });
    // it('should encode query string', function () {
    //   const query = { key1: 'value1', key2: 'value2' };
    //   const req = new HTTPRequest({ baseURL, query });
    //   req.url.should.eql(baseURL + '?key1=value1&key2=value2');
    // });
  });
});
